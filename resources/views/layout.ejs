<!DOCTYPE html>
<html lang="en">
<head>
    
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Realtime pizza app</title>
    <link href="https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700&display=swap" rel="stylesheet">
    <link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css" type="text/css" rel="stylesheet">
    <link rel="stylesheet"
        href="https://maxst.icons8.com/vue-static/landings/line-awesome/line-awesome/1.3.0/css/line-awesome.min.css">
    <link rel="stylesheet" type="text/css" href="/css/app.css">
    <link rel="stylesheet" href="https://maxst.icons8.com/vue-static/landings/line-awesome/font-awesome-line-awesome/css/all.min.css">

</head>
<body>

    <nav class="container flex mx-auto item-center justify-between py-4">
        <div >
            <a href="/"> 
                <img src="/img/logo.png" alt="not">

            </a>
            
        </div> 
        <div>
            <ul class="flex item-center my-3">
                <li class="ml-6"> <a href="/">Menu</a></li>
                <li class="ml-6"> <a href="/order">Orders</a></li>
                <% if(user){ %>


                <li class="ml-6">
                      <form id="logout" action='/logout' method="POST">
                          <a onclick="document.getElementById('logout').submit()" href="#">Logout</a>
                      </form>
                    </li>
               

                  <% } else {  %>
                    <li class="ml-6">  <a href="/Register">Register</a></li>
                    <li class="ml-6">  <a href="/login">Login</a> </li>
                    
                   <% } %>
                <li class="ml-6">  <a href="/cart" class="inline-block px-4 py-1 rounded-full flex">
                    <span id="cartcounter" class="text-white font-bold pr-1"><%= session.cart ? session.cart.totalqty : '' %></span>
                    <img src="/img/cart.png" alt="">
                </a> </li>
            </ul>
        </div>
    </nav>

    <%- body %>
     
     

     <footer></footer>
     <script src="/socket.io/socket.io.js"></script>
    <script src="/js/app.js"></script>
</body>
</html>
<!-- <div>${ renderItems(order.items) }</div> -->
<!-- function renderItems(items) {``
    let parsedItems = Object.values(items)
    // console.log(items)
    return parsedItems.map((menuItem) => {
        return `
            <p>${ menuItem.item.name } - ${ menuItem.qty } pcs </p>
        `
    }).join('')
  } -->
